export enum ActionButtons {
  ALL = "all",
  INVITE = "invite",
  LEAVE = "leave",
  REQUEST = "request",
  REMOVE = "remove",
  UPDATE = "update",
}

export enum AdminRoleMode {
  APPOINT = "appoint",
  REMOVE = "remove",
  BOTH = "both",
}
