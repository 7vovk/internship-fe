export enum Routes {
  ABOUT = "/about",
  AUTH0_LOGOUT = "/auth/logout",
  COMPANIES = "/companies",
  CREATE = "/create",
  HEALTH = "/health",
  HOME = "/",
  LOGIN = "/login",
  PROFILE = "/profile",
  USERS = "/users",
}
